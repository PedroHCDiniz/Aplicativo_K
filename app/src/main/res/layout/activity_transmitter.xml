<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:id="@+id/textRoleLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/transmitter_role_label"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginBottom="16dp" />

        <TextView
            android:id="@+id/textStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/status_offline"
            android:textSize="16sp"
            android:layout_marginBottom="24dp" />

        <Button
            android:id="@+id/buttonGoOnline"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_go_online"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/buttonStartSharing"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_start_sharing"
            android:enabled="false"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/buttonStopSharing"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_stop_sharing"
            android:enabled="false"
            android:layout_marginBottom="12dp" />

        <!-- Gravacao local: independente da transmissao ao vivo (feature extra
             alem do MVP original, pedida separadamente). -->
        <Button
            android:id="@+id/buttonToggleRecording"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_start_recording"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/buttonResetConfig"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_reset_config" />

    </LinearLayout>
</ScrollView>
